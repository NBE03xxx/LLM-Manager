#!/usr/bin/python3 -I
"""Root-owned isolated entry point for the separately installed remote helper."""

import sys

sys.dont_write_bytecode = True
sys.path.insert(0, "/usr/lib/llm-manager-remote-helper")

from llm_manager.infrastructure.remote_helper_cli import main


if __name__ == "__main__":
    raise SystemExit(main())
